# На майбутнє

* semantics List(path, depth) (зараз фактично depth == 1);
* нормалізацію path (\ vs /, trailing slash);
* чи треба Remove() для директорій рекурсивним;
* індекси (якщо з’являться пошуки не тільки по path).

